Vending Machine:

Object

number nickels
number dimes
number quarters

number current_amount

hash cola {cost: $1.00, amount: #}
hash chips {cost: $0.50, amount: #}
hash candy {cost: $0.65, amount: #}

function insert(coin)
	accepts valid coins
	returns invalid coins (anything else)

function select(product)
	dispenses product
	asks for more money

function return()
	returns entered coins